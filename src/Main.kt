fun main() {
    val str = "ABC"
    //backTrackingProblemSolve(str,"",0)

    // val Queen = NQueenProblem()
    // Queen.solveNQ()


    /* val sudokuBoard = arrayOf(
         intArrayOf(5, 3, 0, 0, 7, 0, 0, 0, 0),
         intArrayOf(6, 0, 0, 1, 9, 5, 0, 0, 0),
         intArrayOf(0, 9, 8, 0, 0, 0, 0, 6, 0),
         intArrayOf(8, 0, 0, 0, 6, 0, 0, 0, 3),
         intArrayOf(4, 0, 0, 8, 0, 3, 0, 0, 1),
         intArrayOf(7, 0, 0, 0, 2, 0, 0, 0, 6),
         intArrayOf(0, 6, 0, 0, 0, 0, 2, 8, 0),
         intArrayOf(0, 0, 0, 4, 1, 9, 0, 0, 5),
         intArrayOf(0, 0, 0, 0, 8, 0, 0, 7, 9)
     )

      val solver = SudokuSolver(sudokuBoard)
      if (solver.solve()) {
          solver.printBoard()
      } else {
          println("No solution exists")
      }
 */

    // alphaBatPattern()
    //  kgCalculationLogic()

    /* val n = 10 // Change n to generate Fibonacci series up to nth term
     println("Fibonacci series up to $n terms:")
     for (i in 0 until n) {
         print("${fibonacci(i)} ")
     }
 */
    // doublePeramed()

    /*val start = 50
    val end = 100
    val palindromeSeries = generatePalindromeSeries(start, end)
    println("Palindrome series between $start and $end: $palindromeSeries")*/

    /*val array = intArrayOf(64, 3, 25, 12, 22, 11, 90)
    println(array.size)
    bubbleSort(array, array.size)
    print("Sorted array:${array.joinToString()}")*/

   /* val values = mutableListOf(5, 2, 8, 1, 9, 3)
    val sortedValues = sortValuesRecursively(values)
    println(sortedValues) // Output: [1, 2, 3, 5, 8, 9]*/


    eightPattern()

}



